# design_patterns
design_patterns c++ code
# env
```shell
# init Gtest
git submodule update --init --recursive
```
# Usage
```shell
# project root path
mkdir build && cd build
cmake ../
make -j
./test/alg_ut
```