# base_gtest_project
a simple and basic gtest project
# env
```shell
# init Gtest
git submodule update --init --recursive
```
# Usage
```shell
# project root path
mkdir build && cd build
cmake ../
make -j
./test/alg_ut
```